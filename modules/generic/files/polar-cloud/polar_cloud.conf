[polar_cloud]
# Polar Cloud Server Configuration
server_url = wss://printer4.polar3d.com

# User Credentials (set these through the UI)
username = 
pin = 
serial_number = 

# Printer Configuration
machine_type = Cartesian
printer_type = Cartesian

# Image Settings
max_image_size = 150000

# Debug Settings
verbose = false

# Connection Settings
status_interval = 60 